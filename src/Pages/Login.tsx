import React, { FC } from 'react';
import { Authorization } from '../Components/Authorization';

export const Login: FC = () => {
    return (
        <>
            <div>Авторизация</div>
            <Authorization />
        </>
    );
};
